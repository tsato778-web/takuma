# 05. 確認事項・判断が必要な論点

要件定義書 v1 を読み込んだうえで、**設計を確定させるために回答が必要な項目**を整理した。
A 群は Phase 1 着手前に決めたいもの、B 群は該当スプリント開始までに決めればよいもの、C 群は運用ポリシーとして別途決めるもの。

**A 群は 2026-08-17 に全項目の回答を頂き、設計へ反映済み（第1節）。**

---

## A0. 認証方針の変更（2026-08-17 追加決定）

**本部3名で共通の管理者アカウント1つ（ID＋パスワード）を使う。** 個人別アカウント発行・ユーザー別権限管理・院長/オーナー別ログイン・マルチテナントは現時点では作らない。

| 項目 | 内容 |
| --- | --- |
| 決定理由 | NAORU 内部で実際に使えるものを最短で完成させることを優先し、認証まわりに開発時間をかけない |
| 実装 | Auth.js の Credentials プロバイダ。ID とパスワードは環境変数（`ADMIN_USER_ID` / `ADMIN_PASSWORD_HASH`）。パスワードは scrypt ハッシュで保管し、連続失敗時はログインを一時停止 |
| A-3 / A-4 への影響 | A-3（院長・オーナーは非ログイン）は維持。**A-4（3名に個別アカウント発行）は本決定で置き換え**、3名は `users` に「ログインしない担当者（面談担当・評価者）」として登録する |
| 監査ログ | 「誰が操作したか」は共通アカウント単位の記録になる（個人の特定はできない） |
| 将来の拡張 | providers を追加し `users.canLogin` で許可制にするだけで個人別ログインへ移行できる。テーブル構造の変更は不要 |
| 運用上の注意 | パスワードの共有範囲を本部3名に限定し、定期的に変更する |

---

## A. 決定事項（2026-08-17 回答・設計反映済み）

| # | 論点 | 決定 | 設計への反映先 |
| --- | --- | --- | --- |
| A-1 | 既存 LINE アカウントの友だち | **新規に採用専用アカウントを開設し、既存アカウントから誘導配信を行う。** 既存 LINE はセミナー・アカデミー告知等にも使用しているため統合しない | `03` 第7節（誘導URL・「既存LINE」タグ・移行期間・効果測定）、`04` Sprint 8 |
| A-2 | LIFF（LINEログインチャネル） | **採用する。** 初回アンケート・応募フォームともに LIFF で提供 | `03` 第1・6節、`04` Sprint 3 |
| A-3 | 院長・オーナーの利用 | **Phase 1 は本部が代理入力。** 院長・オーナーはログインしない | `02` `users.can_login`、`04` Sprint 4 |
| A-4 | アカウント発行 | ~~3名に個別発行~~ → **A0 の決定で共通アカウント1つに変更**。3名は「ログインしない担当者」として登録 | `02` 初期データ、`04` Sprint 0 |
| A-5 | 年度の定義 | **内定日ベース・暦年（1〜12月）を既定**とし、**入社日ベース年度（4月始まり）へ切替可能**にする | `02` 判断5・`offer_year` / `join_fiscal_year`、`04` Sprint 6 |
| A-6 | 資格の複数保持 | **複数選択可**（`candidates.licenses` 配列） | `02` `candidates` |
| A-7 | 希望エリアの粒度 | **都道府県＋希望エリア＋自由記述の3層**、**第2希望まで**登録可。**エリアマスタは最小構成で開始し、管理画面から追加・編集可能に** | `02` `candidate_desired_areas` / `areas`、`04` Sprint 3・5 |
| A-8 | LINE 非経由の応募 | **手動登録に対応**（求人媒体・人材紹介・紹介・学校）。後から LINE を追加した場合に**既存候補者と統合（マージ）できる**設計にする | `02` 判断4・`candidate_merge_logs`、`04` Sprint 2・3 |
| A-9 | 既存候補者データの移行 | **選考中の候補者のみ移行。** 過去データ全件は不要。ただし**将来 CSV インポートで追加できる構造**にする | `02` `import_batches` / `import_rows`、`04` Sprint 8 |

### 決定に伴う Phase 1 スコープの変更

| 追加項目 | 対象 | 追加工数 |
| --- | --- | --- |
| 候補者の手動登録＋統合（マージ）機能 | A-8 | +4日 |
| 選考中候補者の CSV インポート | A-9 | +3日 |
| 年度軸の切替（内定年 ⇔ 入社年度） | A-5 | +2日 |
| 希望エリア第2希望＋エリアマスタ管理画面 | A-7 | +2日 |

**Phase 1 の期間見積：約8.5週 → 約10週**（開発1名フルタイム相当）

### 決定から派生した確認事項（Sprint 開始までに確認）

| # | 内容 | 必要時期 |
| --- | --- | --- |
| A-1a | 既存 LINE での誘導配信を**何回・どの間隔で**行うか（例：初回・1週後・2週後・最終案内の4回）。また既存 LINE 側でセグメント配信が可能か（採用対象外の友だちにも届くため） | Sprint 8 |
| A-5a | 「入社年度」の年度境界は**4月始まり**でよいか（1月始まりにしたい場合は要指示） | Sprint 6 |
| A-7a | 初期投入するエリアの一覧（**採用対象の都道府県＋既に紹介実績のあるエリアのみ**で可） | Sprint 5 |
| A-8a | 手動登録時の**必須項目**をどこまで絞るか（氏名のみ必須とし、他は後から追記できる形を推奨） | Sprint 2 |
| A-8b | 統合候補の検出キーは**氏名・電話番号・メールアドレス**の一致でよいか | Sprint 3 |
| A-9a | 移行対象（選考中候補者）の**現在の管理場所と件数**（スプレッドシート／Notion 等、おおよその件数） | Sprint 8 |

---

## B. 該当スプリントまでに確定したい（機能仕様）

| # | 論点 | 補足 |
| --- | --- | --- |
| B-1 | 面談希望日時（第3希望）から**確定日時を決める運用** | 誰が確定し、誰が候補者へ返信するか。Phase 1 は手動返信＋面談レコード作成を想定。自動調整は Phase 2 |
| B-2 | 二次面談＋店舗見学の**日程調整の担当** | 本部が調整するか、院長が直接候補者と連絡するか。後者の場合、院長への情報連携手段が必要 |
| B-3 | 面談の**キャンセル・リスケ**の扱い | 履歴として残すか、上書きするか（設計上は `status='canceled'` で履歴を残す想定） |
| B-4 | 新卒特有の項目の要否 | 要件5には学校名・卒業予定年度・国家試験の受験状況がない。新卒管理に必要なら項目追加。**A-8 で学校経由の応募を管理対象としたため、学校名は流入元としても必要になる可能性が高い** |
| B-5 | 年齢 vs 生年月日 | 年齢のみだと翌年度に古くなる。生年月日での保持を推奨（表示は年齢） |
| B-6 | 「動画視聴対象」タグの定義 | 未視聴者への再送用か、視聴完了者の識別か。LINE の `videoPlayComplete` で自動判別可能 |
| B-7 | リッチメニューの**タブ切替運用**の有無 | 複数メニューを切り替える場合、リッチメニューエイリアスの登録が必要 |
| B-8 | 東京セミナー（要件18）の管理粒度 | 参加有無フラグのみか、開催日程マスタと紐付けるか |
| B-9 | 通知先 | 新規応募・未対応トーク・配信失敗の通知を Slack／メールのどちらに送るか。Slack ならワークスペースと投稿先チャンネル |
| B-10 | Zoom 録画URL の入力運用 | Phase 1 は手入力。Phase 2 で Zoom API 連携（自動取得）を行うかの意向 |
| B-11 | 一次面談の担当を佐藤さん固定にするか | 将来的に複数名で分担する可能性があるなら、担当者アサイン機能を Phase 1 に含める |

---

## C. 運用・法務ポリシーとして決めて頂きたい

| # | 論点 | 補足 |
| --- | --- | --- |
| C-1 | **個人情報の利用目的・同意文面** | 応募フォームに掲載する。顔写真・家庭状況・給与といった機微な情報を扱うため必須 |
| C-2 | **データの保存期間と削除ポリシー** | 例：不採用・辞退から2年経過後に個人特定情報を削除（統計データは残す）。削除の自動化が必要かを判断 |
| C-3 | **顔写真の必須／任意** | 必須にすると応募のハードルが上がる。任意（推奨）を提案 |
| C-4 | **給与情報の閲覧制限** | 初期利用者3名のみのため制限不要と判断しているが、将来的に院長・オーナーがログインする場合は要検討 |
| C-5 | **LINE 公式アカウントの料金プラン** | 配信通数の見込み（友だち数 × 月間配信回数）からプランを選定。シナリオ配信開始時にコストが跳ねる |
| C-6 | **アカウント認証（認証済アカウント）の申請** | 検索結果への表示など集客面のメリットがある |
| C-7 | **障害時の連絡経路** | 配信停止・Webhook 停止時に誰が一次対応するか |

---

## D. 設計側からの提案（要件書に明示されていないが入れておきたいもの）

| # | 提案 | 理由 |
| --- | --- | --- |
| D-1 | **フェーズ滞留日数の可視化** | 「一次面談待ちのまま10日経過」といった停滞を検知できる。要件19の目的（現状把握）を強化する。実装コストは小さい |
| D-2 | **未対応トークの24時間アラート** | LINE 経由の候補者は返信が遅れると離脱しやすい |
| D-3 | **候補者詳細のタイムライン統合表示** | メッセージ・フェーズ変更・タグ付与・評価・紹介を時系列で1本にまとめる。引き継ぎが速くなる |
| D-4 | **一覧からの一括操作** | 複数候補者へのタグ付与・フェーズ変更。運用負荷を大きく下げる |
| D-5 | **監査ログ** | 個人情報を扱うシステムとして最低限必要。後付けは難しいので Phase 1 に含める |
| D-6 | **`LINE_DRY_RUN` モード** | 開発・検証中に実際の候補者へ誤送信する事故を防ぐ |
| D-7 | **面談評価の入力リマインド** | 評価データが揃わないと要件24の入社後分析が成立しない。未入力の面談を一覧表示する |

---

## 次のアクション

1. ~~設計ドキュメントの確認~~ → 完了
2. ~~**A 群（A-1〜A-9）** への回答~~ → 完了（2026-08-17）
3. ~~回答の設計反映~~ → 完了
4. ~~Sprint 0（基盤構築）~~ → 完了（認証方針の変更 A0 を反映済み）
5. **Sprint 1（LINE連携基盤）** ← いまここ。LINE 公式アカウントの設定完了待ち
6. 以降は `04` のスプリント計画のとおり

B 群・C 群は該当スプリントの開始までに回答を頂ければ進行に影響しない。
