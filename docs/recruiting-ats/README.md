# NAORU 採用LINE・ATSシステム 設計ドキュメント

要件定義書 v1 第27章の指示に基づき、**実装に着手する前の設計フェーズの成果物**をまとめたもの。
人間側のレビュー・承認を経てから Phase 1 の実装を開始する。

**ステータス**：2026-08-17 に確認事項 A 群（9項目）の回答を頂き、設計へ反映済み。**最終確認待ち**（実装は未着手）。

## ドキュメント一覧

| # | ドキュメント | 内容 | 要件書の対応 |
| --- | --- | --- | --- |
| 00 | [要件定義書 v1（原文）](./00-requirements.md) | 提示された要件の保存 | 全章 |
| 01 | [技術構成とシステムアーキテクチャ](./01-architecture.md) | 技術選定・比較検討・全体構成図・非同期処理・セキュリティ・環境・コスト | 開発方針 2・3 |
| 02 | [データベース設計とテーブル関連](./02-data-model.md) | ER図・全テーブル定義・要件との対応表・Prismaスキーマ草案・初期データ | 開発方針 4・5 |
| 03 | [LINE Messaging API 連携設計](./03-line-integration.md) | チャネル構成・Webhook設計・イベント処理・送信設計・リッチメニュー・LIFF・運用チェックリスト | 開発方針 6 |
| 04 | [Phase 1 実装計画](./04-phase1-plan.md) | スコープ・スプリント計画・画面一覧・準備物・品質基準・リスク | 開発方針 7 |
| 05 | [確認事項・判断が必要な論点](./05-open-questions.md) | **A群＝決定事項（回答反映済み）**、B/C群＝今後の確認事項、設計側からの提案 | — |

---

## サマリ（3分で読める要約）

### 技術構成の結論

**TypeScript / Next.js（App Router）＋ PostgreSQL（Supabase）＋ Prisma / Vercel** の単一リポジトリ構成。管理画面・API・Webhook・LIFFフォーム・バッチをすべて1つのアプリで持ち、1〜2名で保守できる規模に収める。既存の NAORU ダッシュボードと同じ Vercel 運用のため、インフラ運用の学習コストが増えない。

比較検討した Rails 案・API分離案よりも、**本件の規模（利用者3名・機能数は多いが同時アクセスは少ない）に対して構成が単純**である点を重視した。

### 設計上の重要な5つの判断

1. **LINE友だちと候補者を1つの人物レコードに統合する** — 友だち追加時点で候補者レコードを作成し、フェーズ「LINE登録」から管理する。応募前の見込み候補者も同じ仕組みでタグ付け・検索・配信対象にできる。
2. **採用フェーズ・タグ・フォーム項目をすべて DB のデータとして持つ** — フェーズ追加やアンケート項目の変更を、開発なしで管理画面から行える。集計は表示名ではなく機械可読な `kind` / `funnel_step` を参照するため、名称変更が分析を壊さない。
3. **フェーズ遷移を必ず履歴に残す** — 現在値だけを保持すると要件20の転換率も要件24の入社後分析も算出できなくなるため、`candidate_stage_histories` を Phase 1 の必須要素とした。
4. **登録経路を分離し、後から LINE と統合できるようにする**（A-8）— 求人媒体・人材紹介・学校経由の応募者も同じ候補者テーブルで管理し、後日その人が LINE を友だち追加したら統合できる。誤統合を防ぐため自動統合はせず、候補提示 → 担当者確認 → 取り消し可能な統合とする。
5. **年度の集計軸を2つ持つ**（A-5）— 既定は「内定日ベース・暦年（1〜12月）」、切替で「入社日ベース・年度（4月始まり）」。1つの列では両立できないため `offer_year` / `join_fiscal_year` を分けて保持する。

### LINE 連携の要点

- Webhook は「署名検証 → 生ログ保存 → 即200 → 非同期処理」の4段構成。LINE の再送・重複に耐え、障害後の再処理もできる。
- **LIFF を採用**（決定 / A-2）。リッチメニューの URL は全友だち共通のため、LIFF なしでは「リッチメニュー → 応募フォーム」で回答者を特定できない。LINEログインチャネルの追加作成が必要。
- LINE 公式アカウント側の「あいさつメッセージ」「応答メッセージ」はオフにする（システム側と二重になるため）。
- **既存 LINE アカウントの友だちリストは新アカウントへ移行できない**。既存 LINE は採用以外の用途にも使用しているため統合はせず、**誘導配信で移行する**（決定 / A-1）。移行者には「既存LINE」タグを自動付与して効果を測定する。

### Phase 1 の見通し

準備1週 ＋ 開発8週 ＋ 受け入れ1週 ＝ **約10週間（開発1名フルタイム相当）**。
要件26の Phase 1 項目に加え、A 群の決定で追加した手動登録・統合・CSVインポート・年度軸切替を含む。自動配信は Phase 2 だが、テンプレート＋1対1トークを Phase 1 に入れることで、手動運用でも実務が回る状態でリリースする。

---

## レビューして頂きたいこと

A 群への回答は反映済み。以下の**最終確認**が取れ次第、Sprint 0（基盤構築）から実装に入る。

1. **反映内容**（`05` 第1節の決定事項一覧）— 意図どおりに反映されているか
2. **Phase 1 の期間**（約8.5週 → **約10週**）— スコープ追加に伴う延長で問題ないか
3. **派生した確認事項**（`05` の A-1a〜A-9a の6項目）— 各スプリント開始までに回答が必要
